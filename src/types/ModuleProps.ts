import { RouteProps } from 'react-router'

type ModuleProps = {
    routeProps: RouteProps
    name: string
    link: string
    stateTypes: any
    storeKey: string
    store: any
}

export default ModuleProps
